services:
  engine:
    build: .
    image: vera-engine
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - PIONEER_API_KEY=${PIONEER_API_KEY:-}
    env_file:
      - path: .env
        required: false
